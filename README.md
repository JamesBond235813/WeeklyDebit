# WeeklyDebit

Current project snapshot.

## Structure

- `recycle-ops/`: frontend application.
- `silver-union-all/`: backend service.

Sensitive runtime configuration files such as `bootstrap*.yml`, local env files,
build outputs, dependency directories, and logs are intentionally excluded from
this snapshot.
